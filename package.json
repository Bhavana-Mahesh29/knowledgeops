{
  "name": "knowledgeops",
  "version": "1.0.0",
  "description": "Detects when support agents have drifted away from a knowledge-base article's documented path, drafts a validated patch, and publishes it back to Freshdesk after review.",
  "scripts": {
    "test": "node test.js && vitest run",
    "test:logic": "node test.js",
    "test:watch": "vitest"
  },
  "dependencies": {},
  "devDependencies": {
    "@vitest/coverage-v8": "~2.1.8",
    "jsdom": "~26.1.0",
    "vite": "~5.0.0",
    "vitest": "~2.1.8"
  }
}
