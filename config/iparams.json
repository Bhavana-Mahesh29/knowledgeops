{
  "freshdesk_domain": {
    "display_name": "Freshdesk domain",
    "description": "Your full Freshdesk domain, e.g. yourcompany.freshdesk.com (no https://, no trailing slash).",
    "type": "text",
    "required": true
  },
  "freshdesk_api_key": {
    "display_name": "Freshdesk API key",
    "description": "From your Freshdesk profile settings. Needed to read tickets and conversations and to publish article updates.",
    "type": "text",
    "secure": true,
    "required": true
  },
  "anthropic_api_key": {
    "display_name": "Anthropic API Key",
    "description": "Used for step extraction (Haiku) and patch drafting (Sonnet). If the key is missing or the call fails, the app falls back to deterministic taxonomy matching and a template patch.",
    "type": "text",
    "secure": true,
    "required": true
  },
  "admin_emails": {
    "display_name": "Approvers (agent emails)",
    "description": "Comma-separated Freshdesk agent emails allowed to approve, edit or reject a patch. Leave blank to allow anyone who can open the app. This controls the buttons, not the data-pipe - restrict who can open the app for a real boundary.",
    "type": "text",
    "required": false
  },
  "demo_mode": {
    "display_name": "Demo mode (relaxed convergence gates)",
    "description": "Raises an alert from a single ticket instead of requiring 4 tickets from 3 different agents. Turn this ON to see the full pipeline from one simulated ticket. Leave OFF in production.",
    "type": "checkbox",
    "default_value": false
  },
  "provision_hedging": {
    "display_name": "Enable provisional-answer hedging (demo only)",
    "description": "When on, the bot may share an unapproved provisional path for Critical alerts. Leave OFF in production.",
    "type": "checkbox",
    "default_value": false
  },
  "publish_mode": {
    "display_name": "Publish mode",
    "description": "demo publishes approved patches immediately; production saves them as drafts for manual release.",
    "type": "dropdown",
    "options": ["production", "demo"],
    "default_value": "production"
  },
  "gap_folder_id": {
    "display_name": "Folder for new articles (knowledge gaps)",
    "description": "Freshdesk solution folder id that approved knowledge-gap drafts are created in. Find it in the folder's URL under Solutions. Required only to approve a knowledge-gap draft.",
    "type": "text",
    "required": false
  },
  "vobiz_auth_id": {
    "display_name": "Vobiz Auth ID",
    "description": "From the Vobiz console. Phone alerts stay off until the four Vobiz/relay fields are filled in.",
    "type": "text",
    "required": false
  },
  "vobiz_auth_token": {
    "display_name": "Vobiz Auth Token",
    "description": "From the Vobiz console. Sent only to api.vobiz.ai.",
    "type": "text",
    "secure": true,
    "required": false
  },
  "vobiz_from_number": {
    "display_name": "Vobiz caller number",
    "description": "The Vobiz number calls are placed from, in international format.",
    "type": "text",
    "default_value": "+918064261580",
    "required": false
  },
  "voice_relay_url": {
    "display_name": "Voice relay URL",
    "description": "Public https URL of scripts/voice-relay.js (e.g. your ngrok URL). Vobiz fetches the spoken message from it when a call connects.",
    "type": "text",
    "required": false
  },
  "admin_phone": {
    "display_name": "Admin phone number(s)",
    "description": "Called when a High Knowledge Drift alert is raised. International format; separate several with commas.",
    "type": "text",
    "required": false
  },
  "dept_heads": {
    "display_name": "Department heads",
    "description": "Called when an approved update reaches Freshdesk. One per line: <folder id, category id or category name> = <team> | <phone>. Add 'default = <team> | <phone>' as a fallback. Example: 68000075564 = IT Support | +919876543210",
    "type": "paragraph",
    "required": false
  },
  "embedding_provider": {
    "display_name": "Embedding provider (ticket-to-article matching)",
    "description": "local = built-in deterministic embedder, no key or network needed. voyage = Voyage AI embeddings (needs the Voyage API key below). Changing provider re-embeds the knowledge base on the next match.",
    "type": "dropdown",
    "options": ["local", "voyage"],
    "default_value": "local"
  },
  "voyage_api_key": {
    "display_name": "Voyage AI API key",
    "description": "Only used when the embedding provider is voyage. Sent to api.voyageai.com to embed knowledge-base articles and the subject + resolution of resolved tickets (after PII redaction).",
    "type": "text",
    "secure": true,
    "required": false
  },
  "semantic_match_threshold": {
    "display_name": "Semantic match threshold",
    "description": "Minimum cosine similarity (0-1) for an article to be shown to the relevance gate. Leave blank for the provider default: 0.80 for voyage, 0.30 for local.",
    "type": "text",
    "required": false
  }
}
